Les 3 commandes de Push
git init nom_du_repository
/!\ Il faut etre dans le fichier qu'on à cloner.
git add .
git commit -m "Inserer une note"
git push

git pull pour mettre à jour le fichier sur le local.
